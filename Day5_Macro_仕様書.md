# Day5_Macro_仕様書

## 📘 1. 概要
本ツールは、Sheet1 の売上データから指定した閾値以上の行を抽出し、  
Sheet2 にコピーした後、A列を昇順に並べ替える Excel VBA マクロです。

---

## ⚙️ 2. 機能仕様（仕様書）

### 2-1. 閾値入力
- InputBox により閾値（売上）を入力します。
- 数字以外が入力された場合は警告を表示し、処理を中断します。

### 2-2. 行抽出（CopyRowsAboveThreshold）
- Sheet1 の C列（売上）を判定対象とします。
- 閾値以上の行のみ Sheet2 にコピーします。
- コピー範囲は A〜C列（3列）。

### 2-3. 並べ替え（SortByColumnAscending）
- Sheet2 の A列を昇順に並べ替えます。
- 見出し行（1行目）は除外します。

### 2-4. 出力
- 処理完了後、「売上整理が完了しました！」と表示します。

---

## 🖥️ 3. 操作手順（操作マニュアル）

### Step 1：データを準備する
- Sheet1 に A〜C列のデータ（商品名、日付、売上）を配置します。
- 1行目は見出し。

### Step 2：マクロを実行する
- Excel の「開発」タブ →「マクロ」→「Day5_Macro」を選択して実行。

### Step 3：閾値を入力する
- 表示された InputBox に売上の閾値を入力します。
  - 例：10000

### Step 4：結果を確認する
- Sheet2 に抽出結果が出力。
- A列が昇順に並べ替えられていることを確認します。

---

## 🧪 4. テスト仕様書

| No | テスト内容 | 入力値 | 期待結果 | 結果 | チェック | 備考 |
|----|------------|--------|-----------|------|----------|------|
| 1 | 正常系：閾値10000 | 10000 | 10000以上の行のみコピーされる | OK | ✅ | 正常動作 |
| 2 | 境界値：閾値5000 | 5000 | 5000以上の行のみコピーされる | OK | ✅ | 境界値も正しく処理 |
| 3 | 異常系：文字入力 | abc | 警告表示、処理中断 | OK | ✅ | 警告表示を確認 |
| 4 | 異常系：空欄入力 | (空欄) | 警告表示、処理中断 | OK | ✅ | 空欄でも警告 |
| 5 | 正常系：少量データ（1行のみ） | 10000 | 正常に動作 | OK | ✅ | 問題なし |
| 6 | 正常系：データなし（見出しのみ） | 10000 | Sheet2は見出しのみ | OK | ✅ | 見出しのみ出力 |
| 7 | 正常系：大量データ（10000行） | 10000 | 高速で処理完了 | OK | ✅ | 処理速度良好 |
| 8 | 正常系：閾値0 | 0 | 全行コピーされる | OK | ✅ | 全行コピーを確認 |
| 9 | 正常系：閾値非常に大きい | 99999999 | 見出しのみ | OK | ✅ | 正常 |
| 10 | 正常系：ソート確認 | 10000 | A列が昇順になる | OK | ✅ | 昇順ソート確認 |

---

## 🐞 5. 不具合管理表

| No | 発生日時 | 発生状況 | 再現手順 | 原因 | 対応内容 | 修正日 | 修正者 | 再テスト結果 | チェック | 備考 |
|----|-----------|-----------|-----------|--------|-----------|-----------|-----------|----------------|---------|--------|
| 1 | 2026/08/02 | テスト5. 少量データ（1行のみ） | 閾値に1000を入力 | ソート処理の失敗 | SortByColumnAscending に If ws.UsedRange.Rows.Count <= 1 Then Exit Function End If を追加 | 2026/08/02 | masalog | OK | ✅ | 見出し1行しかデータがない場合は、コピー後のソートをしないよう修正 |

---

## ⚠️ 6. 注意事項
- 閾値は必ず数値を入力すること。
- Sheet1 のデータ構成（A〜C列）は変更しないこと。
- 大量データ（10000行以上）でも高速に動作するが、Excel の負荷に注意。

---

## 📝 7. 改善履歴
- 2026/08/02：Day5_Macro_仕様書を作成
